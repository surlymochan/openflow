"""
Superflow atom entry points — thin wrappers over existing implementation scripts.

Each module here corresponds to exactly one atom ID in atoms/registry.json.
The implementations live in ../scripts/ and ../*/scripts/ and are NOT duplicated here.
"""
